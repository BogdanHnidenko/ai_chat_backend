// Тут JSDoc-типи для автодоповнення в VS Code (без TypeScript)
// Якщо перейдеш на TypeScript — тут будуть .ts інтерфейси

/**
 * @typedef {Object} User
 * @property {number} id
 * @property {string} name
 * @property {string} email
 * @property {'user' | 'admin'} role
 */

/**
 * @typedef {Object} ApiResponse
 * @property {boolean} success
 * @property {any} data
 * @property {string} [message]
 */

export {}
